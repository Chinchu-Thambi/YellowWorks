import styled from 'styled-components';
import Button from '../../../../../../components/Button';

export const ButtonStyled = styled(Button)`
  padding-left: 0;
  padding-right: 0;
`;

export const WrappingParagraph = styled.p`
  word-break: break-word;
`;
